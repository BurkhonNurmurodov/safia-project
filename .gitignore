# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
build/
.eggs/
venv/
env/
.venv/
*.pyc

# ── Node / Frontend ─────────────────────────────────────
node_modules/
frontend/dist-ssr/
frontend/build/
*.local

# ── Environment / Secrets ───────────────────────────────
.env
.env.*
!.env.example
backend/.env
frontend/.env
bot/.env
# cPanel's File Manager downloads .env copies under names like
# "cPanel File Manager.uu" — a verbatim copy of the PRODUCTION env.
*.uu
cPanel*

# ── Google service account key ──────────────────────────
*.json
!package.json
!package-lock.json
!tsconfig.json
!jsconfig.json
!eslint.config.json
# Defence in depth: keep these ignored even if *.json is ever relaxed.
safia-project-*.json
*service-account*.json
*credentials*.json

# ── Keys / certificates ─────────────────────────────────
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*

# ── Database dumps / backups ────────────────────────────
# The admin panel writes full production dumps (PII + auth tokens) here.
*.sql
*.sql.gz
*.dump
*.bak
*.sqlite
*.sqlite3
*.db

# ── Archives ────────────────────────────────────────────
*.zip
*.tar
*.tar.gz
*.tgz
*.7z

# ── Logs ────────────────────────────────────────────────
*.log
logs/
*.log.*

# ── OS / Editor ─────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.sw?

# ── Misc project artifacts ──────────────────────────────
backend.zip
verifix_data.json
verifix_data.xlsx
verifix_run.log
zi47WZ0K
frontend/dist.tar.gz
frontend/dist.zip

# –– Apps Script –––––––––––––––––––––––––––––––––––––––––
apps-script